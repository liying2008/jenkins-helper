import type { Theme } from './theme'
import { extend } from './base_theme'

export const defaultTheme: Theme = extend({
  name: 'default',
  light: {
    common: {
    },
    custom: {
    },
  },
  dark: {
    common: {
    },
    custom: {
    },
  },
})
